name: Daily News Update

on:
  schedule:
    - cron: '0 23 * * 1-5'   # 07:00 SGT (월-금)
    - cron: '0 1 * * 1-5'    # 09:00 SGT (월-금)
    - cron: '0 3 * * 1-5'    # 11:00 SGT (월-금)
    - cron: '0 5 * * 1-5'    # 13:00 SGT (월-금)
    - cron: '0 7 * * 1-5'    # 15:00 SGT (월-금)
    - cron: '0 9 * * 1-5'    # 17:00 SGT (월-금)
    - cron: '0 14 * * 1-5'   # 22:00 SGT (월-금)
    - cron: '0 10 * * 6'     # 18:00 SGT (토)
    - cron: '0 12 * * 6'     # 20:00 SGT (토)
    - cron: '0 14 * * 6'     # 22:00 SGT (토)
    - cron: '0 16 * * 6'     # 00:00 SGT (일)
    - cron: '0 18 * * 6'     # 02:00 SGT (일)
    - cron: '0 20 * * 6'     # 04:00 SGT (일)
    - cron: '0 22 * * 6'     # 06:00 SGT (일)
    - cron: '0 0 * * 0'      # 08:00 SGT (일)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install anthropic requests beautifulsoup4 pytz

      - name: Fetch news with Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python fetch_news.py

      - name: Commit & push news data
        run: |
          git config user.name "News Bot"
          git config user.email "bot@example.com"
          git add news_data.json
          git diff --staged --quiet || git commit -m "news: update $(date +'%Y-%m-%d %H:%M SGT')"
          git push
